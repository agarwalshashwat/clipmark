# Node.js
node_modules/
npm-debug.log

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
env/
venv/

# Java
*.class
*.jar
*.war
*.ear
target/

# IDEs
.idea/
.vscode/
*.sublime-workspace
*.sublime-project

# OS generated files
.DS_Store
Thumbs.db
.DS_Store?
ehthumbs.db
Icon?

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js
webapp/.next/
webapp/out/

# Production
webapp/build

# Environment variables
.env
webapp/.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Sentry — written by the build plugin / wizard, contains an auth token
.env.sentry-build-plugin
.sentryclirc

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts
.claude/
deep-research-*
clipmark-extension.zip

# Playwright
playwright-report/
test-results/
tests/visual/__snapshots__/
*-snapshots/

# extension/src/config.js is committed as the production default (no secrets —
# just the public API base URL). Override it locally for dev; the extension
# build guard in vite.config.mjs fails the build if API_BASE points at localhost.
extension/dist/
screenshots/
clipmark-extension-*.zip
